import styled from "styled-components";
import { Board } from "./Board";

export const StyledBoard = styled(Board)`
  .board-main {
    width: 100px;
    height: 100px;
    display: flex;
    flex-wrap: wrap;
  }
`;
